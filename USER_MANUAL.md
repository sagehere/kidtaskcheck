# 任务打卡系统用户手册

## 目录

- [1. 系统简介](#1-系统简介)
- [2. 部署指南](#2-部署指南)
  - [2.1 环境要求](#21-环境要求)
  - [2.2 Docker 部署（推荐）](#22-docker-部署推荐)
  - [2.3 本地开发部署](#23-本地开发部署)
  - [2.4 配置反向代理](#24-配置反向代理)
  - [2.5 数据备份与恢复](#25-数据备份与恢复)
  - [2.6 系统更新](#26-系统更新)
- [3. 管理员使用指南](#3-管理员使用指南)
- [4. 家长使用指南](#4-家长使用指南)
- [5. 孩子使用指南](#5-孩子使用指南)
- [6. 核心规则说明](#6-核心规则说明)
- [7. 常见问题解答](#7-常见问题解答)

---

## 1. 系统简介

### 这是什么？

"任务打卡系统"是一套面向家庭的积分激励工具，帮助家长通过任务、奖励、成就和消息反馈，建立清晰的家庭协作与鼓励机制。

### 能做什么？

- **家长**：创建任务让孩子完成，设置奖励让孩子兑换，记录表扬和批评
- **孩子**：查看并完成任务获得积分，用积分兑换奖励，查看成就和消息
- **管理员**：管理家长账号，配置系统参数

### 系统角色

| 角色 | 职责 |
|------|------|
| 管理员 | 维护系统级账号与基础配置 |
| 家长 | 创建和管理孩子账号，配置任务、奖励，审核提交和兑换 |
| 孩子 | 完成任务、兑换奖励、查看积分和成就 |

---

## 2. 部署指南

### 2.1 环境要求

#### Docker 部署（推荐）

- **操作系统**：Windows 10/11、macOS、Linux（如 Ubuntu 20.04+）
- **软件**：[Docker Desktop](https://www.docker.com/products/docker-desktop/)（Windows/Mac）或 Docker Engine（Linux）
- **硬件**：至少 1GB 可用内存
- **网络**：需要能访问互联网下载镜像

#### 本地开发部署

- **Node.js**：版本 20 或更高（[下载地址](https://nodejs.org/)）
- **npm**：随 Node.js 一起安装
- **操作系统**：Windows、macOS、Linux 均可

### 2.2 Docker 部署（推荐）

这是最简单的部署方式，适合不熟悉编程的用户。

#### 第一步：安装 Docker

**Windows 用户：**
1. 访问 https://www.docker.com/products/docker-desktop/
2. 点击 "Download for Windows" 下载安装包
3. 双击运行安装程序，按提示完成安装
4. 安装完成后重启电脑
5. 在开始菜单搜索 "Docker Desktop" 并打开，等待启动完成

**Mac 用户：**
1. 访问 https://www.docker.com/products/docker-desktop/
2. 点击 "Download for Mac" 下载安装包
3. 双击 `.dmg` 文件，将 Docker 图标拖到 Applications 文件夹
4. 在 Applications 中打开 Docker Desktop，等待启动完成

**Linux 用户（Ubuntu）：**
```bash
# 更新包索引
sudo apt update

# 安装必要的依赖
sudo apt install -y apt-transport-https ca-certificates curl software-properties-common

# 添加 Docker 官方 GPG 密钥
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg

# 添加 Docker 仓库
echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

# 安装 Docker
sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin

# 将当前用户添加到 docker 组（免 sudo）
sudo usermod -aG docker $USER

# 重新登录使权限生效
```

#### 第二步：创建项目文件夹

打开终端（Windows 用户打开 PowerShell 或 CMD），执行以下命令：

```bash
# 创建一个文件夹用于存放系统数据
mkdir -p ~/task-check-system
cd ~/task-check-system
```

#### 第三步：创建配置文件

在 `~/task-check-system` 文件夹中创建一个名为 `docker-compose.yml` 的文件，内容如下：

```yaml
services:
  app:
    image: ghcr.io/sagehere/kidtaskcheck:latest
    restart: unless-stopped
    ports:
      - "100:3000"
    environment:
      ENVIRONMENT: production
      ADMIN_USERNAME: admin
      ADMIN_PASSWORD: 你的管理员密码
      APP_URL: https://你的域名.com
      DATABASE_PATH: /app/data/taskcheck.sqlite
      ENABLE_BUILTIN_SCHEDULER: "true"
    volumes:
      - ./data:/app/data
    healthcheck:
      test:
        - CMD
        - node
        - -e
        - fetch('http://127.0.0.1:3000/healthz',{signal:AbortSignal.timeout(4000)}).then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 60s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
```

**重要配置说明：**

| 配置项 | 说明 | 示例 |
|--------|------|------|
| `ADMIN_PASSWORD` | 管理员密码，**必须修改** | `MySecurePass123!` |
| `APP_URL` | 你的网站地址，**必须修改** | `https://task.example.com` |
| `ports` | 端口映射，冒号左边是外部访问端口 | `"100:3000"` 表示通过 100 端口访问 |

#### 第四步：启动系统

在终端中执行：

```bash
# 创建数据文件夹
mkdir -p data

# 下载最新镜像
docker compose pull

# 启动系统
docker compose up -d
```

等待命令执行完成，看到类似以下输出表示启动成功：

```
[+] Running 2/2
 ✔ Network task-check-system_default  Created
 ✔ Container task-check-system-app-1  Started
```

#### 第五步：访问系统

打开浏览器，访问 `http://localhost:100`（如果你修改了端口，请使用你设置的端口）。

首次访问会看到登录页面，使用管理员账号登录：
- 账号：`admin`
- 密码：你在配置文件中设置的密码

### 2.3 本地开发部署

如果你想在本地开发或测试，可以使用以下方式：

#### 第一步：安装 Node.js

1. 访问 https://nodejs.org/
2. 下载 LTS 版本（推荐 20.x 或更高）
3. 运行安装程序，按提示完成安装

验证安装：
```bash
node --version
npm --version
```

#### 第二步：下载项目代码

```bash
# 克隆项目（如果你有 Git）
git clone <项目地址>
cd 任务打卡

# 或者直接下载并解压项目文件到一个文件夹
```

#### 第三步：安装依赖

```bash
npm install
```

#### 第四步：构建前端

```bash
npm run build
```

#### 第五步：启动服务

```bash
# 设置管理员密码（Windows PowerShell）
$env:ADMIN_PASSWORD="你的管理员密码"

# 设置管理员密码（Mac/Linux）
export ADMIN_PASSWORD="你的管理员密码"

# 启动服务
npm run server
```

服务启动后，访问 `http://localhost:3000` 即可。

### 2.4 配置反向代理

如果你需要通过域名和 HTTPS 访问系统，需要配置反向代理。

#### 使用 Nginx Proxy Manager（推荐新手）

1. 安装 Nginx Proxy Manager（也有 Docker 版本）
2. 登录管理界面
3. 添加 Proxy Host：
   - Domain Names: 你的域名（如 `task.example.com`）
   - Scheme: `http`
   - Forward Hostname / IP: `127.0.0.1`
   - Forward Port: `100`（你设置的外部端口）
   - SSL: 申请证书
   - Force SSL: 启用

#### 使用宝塔面板

1. 在宝塔面板中安装 Nginx
2. 添加站点，绑定域名
3. 在站点设置中配置反向代理：
   - 目标 URL: `http://127.0.0.1:100`
4. 在 SSL 选项中申请 Let's Encrypt 证书

#### 使用 Cloudflare Tunnel（无需公网 IP）

1. 注册 Cloudflare 账号
2. 安装 cloudflared
3. 创建隧道并配置域名

### 2.5 数据备份与恢复

#### 备份数据

系统数据存储在 `data` 文件夹中的 SQLite 数据文件里。

**手动备份：**
```bash
# 备份整个 data 文件夹
cp -r data data-backup-$(date +%Y%m%d)
```

**Docker 环境备份：**
```bash
# 创建备份文件夹
mkdir -p backups

# 备份数据库
docker compose exec app node -e "
  import { DatabaseSync } from 'node:sqlite';
  const db = new DatabaseSync('/app/data/taskcheck.sqlite');
  db.exec(\"VACUUM INTO '/app/data/backup.sqlite'\");
  db.close();
"

# 复制备份到宿主机
cp data/backup.sqlite backups/taskcheck-$(date +%F).sqlite
```

#### 恢复数据

将备份的 SQLite 文件复制回 `data/taskcheck.sqlite` 即可。

### 2.6 系统更新

**Docker 环境更新：**
```bash
# 拉取最新镜像
docker compose pull

# 重启服务
docker compose up -d --force-recreate
```

**本地环境更新：**
```bash
# 拉取最新代码
git pull

# 安装依赖
npm install

# 重新构建
npm run build

# 重启服务（先停止旧服务，再启动）
npm run server
```

---

## 3. 管理员使用指南

### 3.1 首次登录

1. 打开浏览器访问系统
2. 输入管理员账号和密码
3. 点击"进入系统"

**重要：首次登录后请立即修改默认密码！**

### 3.2 修改管理员密码

1. 在管理员面板找到"管理员账号设置"
2. 输入当前密码
3. 输入新密码并确认
4. 点击"保存管理员账号"

### 3.3 设置系统时区

时区设置非常重要，它会影响：
- 任务每天/每周/每月的重置时间
- 奖励兑换的周期限制
- 成就统计的时间窗口

**设置步骤：**
1. 在"系统设置"区域找到"重置时区"
2. 选择你所在的时区（中国选择 `UTC+08:00`）
3. 点击"保存系统设置"

### 3.4 创建家长账号

1. 在"家长用户"区域填写：
   - 账号：登录用的用户名（如 `zhangsan`）
   - 显示名：显示的名称（如 `张三爸爸`）
   - 初始密码：设置一个初始密码
2. 点击"创建家长"

### 3.5 管理家长账号

- **重置密码**：点击家长列表中的钥匙图标
- **删除家长**：点击家长列表中的删除图标（会归档，不会丢失数据）

---

## 4. 家长使用指南

### 4.1 首次登录

1. 使用管理员创建的账号密码登录
2. 首次登录后建议修改密码并设置"操作者称谓"

### 4.2 修改密码和称谓

1. 切换到"设置"标签
2. 找到"修改密码"区域
3. 填写：
   - 操作者称谓：如"妈妈"、"爸爸"
   - 当前密码
   - 新密码（如需修改）
   - 确认新密码
4. 点击"保存账号设置"

### 4.3 创建孩子账号

1. 切换到"儿童管理"标签
2. 在"创建孩子"区域填写：
   - 账号：孩子登录用的用户名（如 `child1`）
   - 显示名：显示的名称（如 `小明`）
   - 密码：设置登录密码
3. 点击"创建孩子"

### 4.4 创建任务分类

任务分类帮助你组织不同类型的任务，如"学习"、"劳动"、"品德"、"健康"。

1. 切换到"设置"标签
2. 在"任务配置"区域找到"创建分类"
3. 填写分类名称和图标
4. 点击创建

### 4.5 创建任务

1. 在"任务配置"区域找到"创建任务"
2. 填写任务信息：

| 配置项 | 说明 | 示例 |
|--------|------|------|
| 标题 | 任务名称 | `完成数学作业` |
| 说明 | 详细描述（可选） | `认真完成今天布置的数学练习册` |
| 分类 | 选择所属分类 | `学习` |
| 周期 | 任务重置周期 | `每日` 表示每天重置 |
| 次数限制 | 每周期可提交次数 | `1` 表示每天只能提交 1 次 |
| 积分 | 完成获得的积分 | `10` |
| 生效星期 | 哪些天可以做 | 默认全选，可取消某些天 |
| 图标 | 显示的图标 | 选择一个合适的 emoji |
| 适用孩子 | 分配给哪个孩子 | 勾选对应的孩子 |

3. 点击"创建任务"

**任务周期说明：**
- **每日**：每天 0 点重置，每天可完成指定次数
- **每周**：每周一 0 点重置，每周可完成指定次数
- **每月**：每月 1 号 0 点重置，每月可完成指定次数
- **一次性**：完成后不再出现

### 4.6 创建奖励

1. 在"奖励配置"区域找到"创建奖励"
2. 填写奖励信息：

| 配置项 | 说明 | 示例 |
|--------|------|------|
| 标题 | 奖励名称 | `看动画片30分钟` |
| 说明 | 详细描述（可选） | `可以看30分钟喜欢的动画片` |
| 所需积分 | 兑换需要的积分 | `50` |
| 限制周期 | 兑换周期限制 | `每日` 表示每天只能兑换指定次数 |
| 次数限制 | 每周期可兑换次数 | `1` |
| 核销星期 | 家长可以在哪些天核销 | 只选周末表示只能周末兑现 |
| 前置任务 | 需要先完成的任务 | 可设置需要先完成某任务 N 次 |
| 成就要求 | 需要先解锁的成就 | 可设置需要先解锁某成就 |
| 适用孩子 | 分配给哪个孩子 | 勾选对应的孩子 |

3. 点击"创建奖励"

### 4.7 审核任务

当孩子提交任务后，你会在首页看到"待审核任务"：

1. 查看任务详情
2. 点击"通过"按钮：任务通过，积分到账
3. 点击"驳回"按钮：输入驳回原因，任务不计分

### 4.8 核销奖励

当孩子兑换奖励后，你会在首页看到"待核销奖励"：

1. 查看奖励详情
2. 点击"核销"按钮：确认兑换，奖励完成
3. 点击"取消"按钮：撤销兑换，积分退回

### 4.9 表扬与批评

你可以预先创建表扬和批评模板，然后快速记录孩子的表现：

**创建模板：**
1. 在"设置"标签找到"表扬与批评条款"
2. 创建表扬模板（如"主动做家务"，加 5 分）
3. 创建批评模板（如"不按时睡觉"，扣 3 分）

**使用模板：**
1. 在首页找到"表扬与批评"区域
2. 选择孩子
3. 选择要使用的模板
4. 点击提交

### 4.10 查看孩子积分

在首页点击孩子的积分显示，可以查看完整的积分流水记录。

### 4.11 生成报告

1. 切换到"儿童管理"标签
2. 点击孩子的"报表"按钮
3. 选择报告类型：
   - 打印清单：生成可打印的任务和奖励列表
   - 周报：查看本周的任务完成情况
   - 月报：查看本月的任务完成情况

---

## 5. 孩子使用指南

### 5.1 首次登录

1. 使用家长给的账号密码登录
2. 首页会显示你的积分、成就和任务

### 5.2 完成任务

1. 在"任务"标签查看任务墙
2. 找到要完成的任务
3. 点击"提交完成"按钮
4. 确认提交
5. 等待家长审核

**注意：**
- 提交后积分不会立即到账，需要家长审核通过
- 每个任务有次数限制，达到上限后需要等待重置
- 有些任务只在特定日期显示

### 5.3 兑换奖励

1. 切换到"奖励"标签查看奖励墙
2. 找到想兑换的奖励
3. 点击"兑换"按钮
4. 确认兑换
5. 等待家长核销

**注意：**
- 兑换时积分会先扣除
- 需要家长核销后才算完成
- 如果家长取消，积分会退回

### 5.4 查看仓库

切换到"仓库"标签，可以查看：

- **待核销**：已兑换但家长还没确认的奖励
- **已核销**：家长已确认的奖励

点击"一键清理已核销奖励"可以隐藏已核销的奖励（不会删除记录）。

### 5.5 查看积分

点击首页的积分显示，可以查看积分流水：

- 任务通过获得的积分
- 兑换奖励扣除的积分
- 表扬获得的积分
- 批评扣除的积分

### 5.6 查看成就

在首页的"成就墙"区域，可以查看已解锁的成就称号。

### 5.7 置顶功能

你可以为一个任务和一个奖励各设置一个置顶项，让它们更醒目地显示在页面上。

点击任务或奖励上的图钉图标即可置顶或取消置顶。

### 5.8 查看消息

点击页面顶部的消息图标，可以查看：

- 任务审核结果
- 奖励核销结果
- 表扬和批评消息

部分消息需要点击确认阅读。

---

## 6. 核心规则说明

### 6.1 积分计算

系统中的积分不是存储一个余额数字，而是通过所有积分流水累加计算得出。

例如：
- 任务通过：+10 分
- 兑换奖励：-50 分
- 奖励取消：+50 分（退回）
- 表扬：+5 分
- 批评：-3 分

当前积分 = 所有流水之和

### 6.2 周期重置

任务和奖励的周期按照系统时区计算：

- **每日周期**：每天 00:00 重置
- **每周周期**：每周一 00:00 重置
- **每月周期**：每月 1 号 00:00 重置

### 6.3 审核流程

**任务流程：**
1. 孩子提交任务
2. 进入家长待审核列表
3. 家长通过 → 积分到账
4. 家长驳回 → 不计分，孩子看到原因

**奖励流程：**
1. 孩子兑换奖励
2. 积分立即扣除
3. 进入家长待核销列表
4. 家长核销 → 奖励完成
5. 家长取消 → 积分退回

### 6.4 软删除

系统的删除操作大多是"软删除"，即标记为已删除但不真正删除数据。

这样做的好处：
- 保留历史积分记录
- 保留任务/奖励使用记录
- 避免数据丢失

---

## 7. 常见问题解答

### 部署相关

**Q：Docker 安装失败怎么办？**
A：
- Windows：确保开启了虚拟化（在 BIOS 中设置），确保 Windows 版本支持（需要 Windows 10 64位专业版或更高版本）
- Mac：确保使用的是 Intel 或 Apple Silicon 芯片的 Mac
- Linux：确保使用的是 64 位系统

**Q：启动后无法访问怎么办？**
A：
1. 检查 Docker 是否正在运行
2. 检查端口是否被占用：`netstat -ano | findstr :100`（Windows）或 `lsof -i :100`（Mac/Linux）
3. 检查防火墙是否阻止了访问
4. 查看日志：`docker compose logs -f app`

**Q：忘记管理员密码怎么办？**
A：
1. 停止服务：`docker compose down`
2. 删除数据文件夹中的 `taskcheck.sqlite` 文件（**注意：这会丢失所有数据**）
3. 重新启动服务：`docker compose up -d`
4. 使用默认密码登录并重新设置

### 使用相关

**Q：孩子看不到任务或奖励？**
A：检查以下几点：
1. 任务/奖励是否已分配给该孩子
2. 任务/奖励是否已启用
3. 当前日期是否在任务/奖励的生效星期内
4. 任务/奖励是否已被删除

**Q：任务提交后没有立即加分？**
A：正常现象。任务需要家长审核通过后才会加分。

**Q：兑换奖励后积分先扣了？**
A：正常现象。兑换时积分会先扣除，如果家长取消兑换，积分会退回。

**Q：为什么奖励按钮显示"还要等多久"？**
A：说明该任务或奖励设置了周期限制，当前还没到下一次可提交或可兑换的时间。

**Q：为什么奖励不能核销？**
A：可能原因：
1. 当前不是允许核销的星期
2. 奖励已超出周期上限
3. 前置任务未完成
4. 对应成就尚未解锁

**Q：删除后数据会丢失吗？**
A：不会。系统使用软删除，历史记录会保留。

---

## 附录：快速上手清单

### 管理员首次使用

- [ ] 登录管理员账号
- [ ] 修改管理员密码
- [ ] 设置系统时区
- [ ] 创建家长账号

### 家长首次使用

- [ ] 登录家长账号
- [ ] 修改密码和设置称谓
- [ ] 创建孩子账号
- [ ] 创建任务分类（如：学习、劳动、品德、健康）
- [ ] 创建任务并分配给孩子
- [ ] 创建奖励并设置兑换条件
- [ ] 创建表扬和批评模板（可选）

### 孩子首次使用

- [ ] 登录孩子账号
- [ ] 查看任务墙
- [ ] 完成一个任务并提交
- [ ] 等待家长审核
- [ ] 查看积分变化
- [ ] 尝试兑换一个奖励

---

如有其他问题，请联系系统管理员或查看项目文档。
